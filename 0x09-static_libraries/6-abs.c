#include "main.h"

/**
 * _abs - returns absolute value of a number
 * @n: parameter to be computed
 * Return: absolute values of a parameter will return
 */
int _abs(int n)
{
if (n >= 0)
	return (n);
else
	return (-n);
}
